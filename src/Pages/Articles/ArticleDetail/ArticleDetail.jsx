import useArticles from "../../../Hooks/useArticles";

const ArticleDetail = () => {
    const [articles] = useArticles();
    return (
        <div>
           
        </div>
    );
};

export default ArticleDetail;